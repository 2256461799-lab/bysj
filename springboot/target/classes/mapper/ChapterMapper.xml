<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ChapterMapper">

    <sql id="Base_Column_List">
        id,title,descr,cover,sort_order,status,create_time
    </sql>

    <insert id="insert" parameterType="com.example.entity.Chapter" useGeneratedKeys="true" keyProperty="id">
        insert into chapter
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="title != null">title,</if>
            <if test="descr != null">descr,</if>
            <if test="cover != null">cover,</if>
            <if test="sortOrder != null">sort_order,</if>
            <if test="status != null">status,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="title != null">#{title},</if>
            <if test="descr != null">#{descr},</if>
            <if test="cover != null">#{cover},</if>
            <if test="sortOrder != null">#{sortOrder},</if>
            <if test="status != null">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>

    <delete id="deleteById" parameterType="Integer">
        delete from chapter
        where id = #{id}
    </delete>

    <update id="updateById" parameterType="com.example.entity.Chapter">
        update chapter
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="descr != null">descr = #{descr},</if>
            <if test="cover != null">cover = #{cover},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </set>
        where id = #{id}
    </update>

    <select id="selectById" parameterType="Integer" resultType="com.example.entity.Chapter">
        select
        <include refid="Base_Column_List"/>
        from chapter
        where id = #{id}
    </select>

    <select id="selectAll" resultType="com.example.entity.Chapter">
        select
        <include refid="Base_Column_List"/>
        from chapter
        <where>
            <if test="title != null and title != ''">and title like concat('%', #{title}, '%')</if>
            <if test="status != null and status != ''">and status = #{status}</if>
        </where>
        order by sort_order asc, id desc
    </select>

</mapper>








